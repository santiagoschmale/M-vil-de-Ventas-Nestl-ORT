# Copiar a .env y ajustar si hace falta. Estos valores por defecto
# coinciden con docker-compose.yml, funcionan sin tocar nada.

DATABASE_URL="postgresql://motor:motor@localhost:5432/motor_distribucion?schema=public"

API_PORT=4000
WEB_PORT=5173

# URL que usa el front para llamar a la API
VITE_API_URL="http://localhost:4000"
